name: Prod Build

on:
  push:
    branches:
      - main

jobs:
  build-prod:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Copy .env from server
        run: cp /home/petya/projects/main-nginx/.env .env

      - name: Run make build
        run: make build
